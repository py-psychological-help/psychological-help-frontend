import React from 'react';
import Button from '../buttonHeader/Button';

const Form = () => {
    return (
        <form>
            <input type="text"/>
            <input type="text"/>
            <input type="text"/>
            <Button/>
        </form>
    );
};

export default Form;
